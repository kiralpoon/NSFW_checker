OPENAI_API_KEY=sk-your-key-here
PORT=8080
MAX_FILE_SIZE_MB=25
CORS_ORIGINS=  # Comma-separated list of allowed origins (leave empty for development)
RATE_LIMIT=10/minute  # Rate limit for API endpoints (e.g., "10/minute", "100/hour")
